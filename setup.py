from setuptools import setup, find_packages

setup(
    name='simpsons',
    version='0.0',
    description='Character recognition for simpsons image data',
    author='evren',
    packages=find_packages(),
)
